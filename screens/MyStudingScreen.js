import react, { Component } from 'react';
import { ScrollView, Text } from 'react-native';

class MyStudingScreen extends Component {
  render() {
    return(
      <ScrollView>
        <Text>My studing</Text>
      </ScrollView>
    );
  }
}
